import { RefinementListProps } from "react-instantsearch-hooks-web";
import { StyledRefinementList } from "./elements";

export function RefinementList(props: RefinementListProps) {
  return <StyledRefinementList {...props} />;
}
